def progress_percent(
    done_tasks: int,
    total_tasks: int
) -> float:
    """
    TODO:
    Nếu total_tasks == 0:
        return 0

    Ngược lại:
        return done_tasks / total_tasks * 100
    """
    pass