class PaymentService:

    def can_pay(
        self,
        balance: float,
        order_total: float
    ) -> bool:
        """
        Input:
            balance
            order_total

        Output:
            True nếu đủ tiền
        """
        pass