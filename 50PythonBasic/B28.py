def calculate_salary(
    hours: float,
    rate: float
) -> float:
    """
    TODO:
    Nếu hours <= 40:
        lương = hours * rate

    Nếu hours > 40:
        40 giờ đầu tính bình thường
        số giờ vượt tính 1.5 lần rate
    """
    pass